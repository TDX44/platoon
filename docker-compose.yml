services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    volumes:
      - platoon_data:/data
    restart: unless-stopped

volumes:
  platoon_data:
