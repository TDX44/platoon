services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      DATA_DIR: /data
      HTTPS: "true"
      # The host runs UTC. The app reads dates through app_today() so it is
      # correct either way, but SQLite column DEFAULTs use datetime('now',
      # 'localtime'), which follows the container clock — and container logs
      # are far easier to read against the duty day. PLATOON_TZ must match.
      TZ: America/Chicago
      PLATOON_TZ: America/Chicago
    volumes:
      - ./data:/data
    # Resolve directly against the LAN resolvers instead of inheriting the
    # host's systemd-resolved, which renegotiates with the Tailscale resolver
    # every day or so and returns EAI_AGAIN while it does. The app survives that
    # now (it caches Clerk's JWKS) but there is no reason to ride it out.
    dns:
      - 10.10.50.3
      - 10.10.50.4
      - 1.1.1.1
    restart: unless-stopped

  cloudflared:
    image: cloudflare/cloudflared:latest
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${TUNNEL_TOKEN}
    depends_on:
      - app
    restart: unless-stopped
