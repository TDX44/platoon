SECRET_KEY=change-this-to-a-random-secret
HTTPS=true
CLERK_PUBLISHABLE_KEY=pk_live_or_test_your_publishable_key_here
CLERK_SECRET_KEY=sk_live_or_test_your_secret_key_here
CLERK_FRONTEND_API_URL=https://your-instance.clerk.accounts.dev
CLERK_AUTHORIZED_PARTIES=https://your-app-domain.example.com,http://localhost:5000
# Who may open /admin — the platform operator's dashboard, read-only and
# across every organization on this instance. Comma-separated, case-insensitive.
# The address is only accepted after Clerk confirms it is that account's
# VERIFIED primary email, so CLERK_SECRET_KEY above must be set for it to work.
# Set it to an empty value to turn the dashboard off entirely.
PLATFORM_ADMIN_EMAILS=jonathon.carr5@gmail.com
DB_OWNER_PASSWORD=change-this-to-a-random-password
DB_APP_PASSWORD=change-this-to-a-different-random-password
# Stripe. Dev runs test mode, production live mode; the key and webhook
# signing secret for the OTHER mode may be left empty. With no key for the
# active mode billing is off: every account is complimentary. The two
# variables for the ACTIVE mode are required together — a secret key without
# its webhook secret charges cards the app can never reconcile, and the
# process refuses to boot that way.
STRIPE_MODE=test
STRIPE_TEST_SECRET_KEY=sk_test_your_test_key
STRIPE_TEST_WEBHOOK_SECRET=whsec_your_test_endpoint_secret
STRIPE_LIVE_SECRET_KEY=
STRIPE_LIVE_WEBHOOK_SECRET=
# Whether accounts are billed unless the operator comps them from /admin.
BILLING_DEFAULT=on
# Which hostnames serve the marketing site at '/' instead of the app. Every
# other host — the app subdomain, the LAN address, localhost — keeps serving
# the app there, so getting this wrong costs a visitor the brochure rather
# than a leader the product. Comma-separated, case-insensitive, port ignored.
MARKETING_HOSTS=platoonmanager.com,www.platoonmanager.com
# Where the marketing site's "Sign in" and "Start free trial" send people.
# Only used on a marketing host; anywhere else /app just goes to '/'.
APP_URL=https://app.platoonmanager.com
# The hostname(s) the app answered on before the product had its own domain.
# Retired by 301 rather than switched off, because people have them bookmarked:
# '/' goes to the marketing site, every other path keeps itself and its query on
# APP_URL. /api/ and non-GET are exempt so an open tab's write is not broken
# across origins. Empty to turn the redirect off.
LEGACY_HOSTS=platoon.carr7.com
